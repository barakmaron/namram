const PRODUCT_TYPE = {
    RENT: "rent",
    SALE: "sale"
};

const DISPLAY_TYPE = {
    PRODUCTS: "products",
    CATEGORIES: "categories"
};

const TIME_DATE_FORMAT = "DD/MM/YYYY dddd HH:mm";

const Constants = {
    PRODUCT_TYPE,
    TIME_DATE_FORMAT,
    DISPLAY_TYPE
};

export default Constants;